{
  "name": "distignore",
  "version": "0.1.2",
  "description": "Used with gulp.src tp remove files excluded by .distignore in gulp pipelines.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Dicker",
  "license": "MIT",
  "dependencies": {
    "event-stream": "^4.0.1",
    "minimatch": "^10.0.1"
  }
}
